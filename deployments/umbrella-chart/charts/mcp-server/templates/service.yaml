apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-server-http
  name: mcp-server-http
spec:
  ports:
    - port: {{ .Values.SERVICE.PORT }}
      protocol: TCP
      targetPort: {{ .Values.SERVICE.PORT }}
  selector:
    app: mcp-server-http
  type: {{ .Values.SERVICE.TYPE }}
status:
  loadBalancer: {}