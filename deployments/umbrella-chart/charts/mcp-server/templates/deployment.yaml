apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mcp-server-http
  name: mcp-server-http
spec:
  replicas: {{ .Values.REPLICA_COUNT }}
  selector:
    matchLabels:
      app: mcp-server-http
  strategy: {}
  template:
    metadata:
      labels:
        app: mcp-server-http
    spec:
      containers:
        - image: {{ .Values.IMAGE.REPOSITORY }}:{{ .Values.IMAGE.TAG }}
          name: server
          resources: {}
status: {}